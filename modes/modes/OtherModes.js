// OtherModes.js - Now empty, classes moved to individual files

// OtherModes.js - Classes moved to individual files
// Export for use in other modules
if (typeof module !== 'undefined' && module.exports) {
    module.exports = {};
}

// Make available globally for browser usage
if (typeof window !== 'undefined') {
    // Classes are now defined in their individual files
}